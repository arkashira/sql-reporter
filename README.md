# SQL Reporter

A simple SQL reporter that allows users to create and schedule reports using a drag-and-drop interface.

## Features

* Create reports using a drag-and-drop interface
* Schedule reports to run automatically
* Validate reports for syntax errors

## Usage

1. Create a new report using the drag-and-drop interface
2. Schedule the report to run automatically
3. Validate the report for syntax errors
