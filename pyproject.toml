[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sql-reporter"
version = "0.1.0"
description = "Simple SQL execution mock for Snowflake"
requires-python = ">=3.8"

[tool.pytest.ini_options]
addopts = "-q"
